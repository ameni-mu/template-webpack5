<header class="header">
  <div class="container">
    <h1 class="logo">ロゴ</h1>
    <nav class="g-nav">
      <ul class="g-nav__items">
        <li class="<%= currentPage === 'home' ? 'is-current' : '' %>">
          <a href="#">HOME</a>
        </li>
        <li class="<%= currentPage === 'profile' ? 'is-current' : '' %>">
          <a href="#">Profile</a>
        </li>
        <li class="<%= currentPage === 'works' ? 'is-current' : '' %>">
          <a href="#">Works</a>
        </li>
        <li class="<%= currentPage === 'contact' ? 'is-current' : '' %>">
          <a href="#">Contact</a>
        </li>
      </ul>
    </nav>
  </div>
</header>